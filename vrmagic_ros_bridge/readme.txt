Thes package contains a rosnode, which is the server to the
D3/D2 - Smartcams  and a client API to send compressed Images
to the server.

The rosnode-server publishes the received images in the
ros-network.
